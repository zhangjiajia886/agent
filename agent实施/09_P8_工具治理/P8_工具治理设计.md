# P8 工具治理设计

> 所属阶段：P8  
> 状态：待实施  
> 目标：建立工具能力元数据、风险等级、审批策略、fallback、健康检查和成本统计体系。

---

## 1. 背景

当前 `ToolRegistry` 已有 name、description、input_schema、executor_type、executor_config 等字段，但 Planner/Scheduler/Replanner 需要更丰富的工具能力信息。

## 2. 目标

- [ ] 定义工具能力元数据标准。
- [ ] 补充风险等级。
- [ ] 补充输入/输出产物类型。
- [ ] 补充 fallback_tools。
- [ ] 补充并行能力。
- [ ] 补充成本等级和超时配置。
- [ ] 增加工具健康检查。
- [ ] 增加工具统计指标。

## 3. 非目标

- 不重写所有工具。
- 不引入外部工具市场。
- 不在首期接入 MCP。

## 4. 涉及文件

| 文件 | 类型 | 说明 |
|---|---|---|
| `backend/app/models/agent_config.py` | 可能修改 | ToolRegistry 字段或 executor_config 扩展 |
| `backend/app/api/v1/comic_agent.py` | 修改 | 工具配置 API |
| `backend/app/core/comic_chat_agent/tool_executor.py` | 修改 | 工具健康和能力读取 |
| `backend/app/core/comic_chat_agent/task_runtime/planner.py` | 后续使用 | Planner 使用工具元数据 |
| `backend/app/core/comic_chat_agent/task_runtime/replanner.py` | 后续使用 | Replanner 使用 fallback |

## 5. 工具能力元数据

建议首期放入 `ToolRegistry.executor_config.capability`：

```json
{
  "capability": {
    "risk_level": "L1",
    "requires_approval": false,
    "auto_mode_allowed": true,
    "input_artifact_types": ["image"],
    "output_artifact_types": ["video"],
    "fallback_tools": ["jimeng_generate_video"],
    "supports_batch": false,
    "supports_frame": true,
    "supports_parallel": false,
    "max_parallel": 1,
    "required_inputs": ["source_image"],
    "optional_inputs": ["motion_prompt"],
    "cost_level": "high",
    "timeout_seconds": 300
  }
}
```

## 6. 风险等级

| 等级 | 说明 | 示例 |
|---|---|---|
| L0 | 只读安全 | list_dir/read_file 限定目录 |
| L1 | 创作工具 | generate_image/text_to_speech |
| L2 | 写入/外部请求 | write_file/http_request |
| L3 | 命令/代码执行 | bash/python_exec |
| L4 | 禁止工具 | 危险命令、敏感读取 |

## 7. 健康检查

工具健康状态：

```text
healthy
unhealthy
degraded
unknown
```

检查对象：

- ComfyUI `/system_stats`。
- 即梦 API 鉴权配置。
- TTS 服务可用性。
- 本地文件目录可写性。

## 8. 工具统计

建议统计：

- 成功次数。
- 失败次数。
- 平均耗时。
- 最近错误码。
- 成本估算。
- fallback 次数。

## 9. TODO

- [ ] 统一 capability JSON 结构。
- [ ] 为核心工具补充 capability。
- [ ] API 返回工具能力元数据。
- [ ] Planner 读取 capability。
- [ ] Scheduler 读取 risk_level/parallel。
- [ ] Replanner 读取 fallback_tools。
- [ ] 增加工具健康检查接口。
- [ ] 增加工具统计字段或聚合查询。

## 10. 验收标准

- [ ] 每个核心工具都有 input/output artifact 类型。
- [ ] 每个核心工具都有 risk_level。
- [ ] Planner 不再硬编码所有工具能力。
- [ ] 禁用工具不会被 Planner 选中。
- [ ] fallback_tools 可被 Replanner 读取。
