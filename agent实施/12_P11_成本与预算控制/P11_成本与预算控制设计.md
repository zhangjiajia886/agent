# P11 成本与预算控制设计

> 所属阶段：P11  
> 状态：最小实现已落地  
> 目标：限制 Agent 长任务中的 token、工具调用、GPU/API、视频生成和存储成本，防止自动执行失控。

---

## 1. 背景

漫剧 Agent 会调用 LLM、ComfyUI、即梦、TTS、媒体合成等能力。复杂任务或失败重试可能造成资源消耗失控。

## 2. 目标

- [x] 定义任务预算 `TaskBudget`。
- [x] 定义预算消耗记录 `BudgetUsage`。
- [x] LLM 调用后记录 token 消耗。
- [x] 工具调用前检查工具预算（`pre_tool_check`）。
- [x] 视频/图片/音频生成专项预算检查。
- [x] 预算不足时 block，高成本超限时 ask_user。
- [x] final_report.metadata.budget_usage 包含完整消耗摘要。

## 3. 非目标

- 首期不做真实支付扣费。
- 首期不做复杂套餐系统。
- 首期成本可估算，不要求精确财务计费。

## 4. 涉及文件

| 文件 | 类型 | 说明 |
|---|---|---|
| `backend/app/core/comic_chat_agent/budget.py` | 新增 | BudgetController |
| `backend/app/core/comic_chat_agent/agent_runner.py` | 修改 | LLM/tool 前检查预算 |
| `backend/app/core/comic_chat_agent/tool_executor.py` | 修改 | 工具成本上报 |
| `backend/app/models/agent_task.py` | 可能修改 | 增加 budget/usage 字段 |
| `backend/app/models/agent_config.py` | 可能修改 | ToolRegistry 成本字段 |

## 5. TaskBudget

```python
@dataclass
class TaskBudget:
    max_iterations: int
    max_tool_calls: int
    max_calls_per_tool: dict[str, int]
    max_input_tokens: int
    max_output_tokens: int
    max_image_generations: int
    max_video_generations: int
    max_audio_generations: int
    max_duration_seconds: int
    max_estimated_cost: float
```

## 6. BudgetUsage

```python
@dataclass
class BudgetUsage:
    iterations: int = 0
    tool_calls: int = 0
    calls_per_tool: dict[str, int] = field(default_factory=dict)
    input_tokens: int = 0
    output_tokens: int = 0
    image_generations: int = 0
    video_generations: int = 0
    audio_generations: int = 0
    duration_seconds: int = 0
    estimated_cost: float = 0.0
```

## 7. 预算检查流程

```text
准备调用工具/LLM
  ↓
BudgetController.check(...)
  ├── allow → 执行
  ├── warn → 执行但记录 warning
  ├── ask_user → 请求用户确认继续消耗
  └── block → 阻止执行
```

## 8. 成本估算

工具成本等级：

| 工具类型 | 成本等级 | 示例 |
|---|---|---|
| read/list/grep | low | 本地只读 |
| text_to_speech | medium | TTS |
| generate_image | medium/high | ComfyUI/即梦图片 |
| image_to_video | high | 视频生成 |
| merge_media | medium | ffmpeg 合成 |
| LLM | variable | token 计费 |

## 9. 预算事件

```json
{
  "type": "budget_warning",
  "task_uid": "task_xxx",
  "usage": {
    "video_generations": 2,
    "max_video_generations": 2
  },
  "message": "视频生成次数已达预算上限"
}
```

## 10. TODO

- [x] 新建 `budget.py`。
- [x] 定义 `TaskBudget` 和 `BudgetUsage`。
- [x] 在工具调用前检查工具次数和成本。
- [x] 高成本工具超预算时 ask_user/block。
- [x] 将 usage 写入 final_report.metadata.budget_usage。
- [ ] 前端展示预算 warning。
- [ ] budget_warning 事件流推送到前端。

## 11. 验收标准

- [x] 超过单工具调用上限时阻止继续调用。
- [x] 超过视频生成预算时进入 ask_user。
- [x] final_report 包含预算消耗摘要（budget_usage）。
- [ ] 前端展示预算状态。
