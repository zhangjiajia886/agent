# FastAPI 核心
fastapi>=0.130.0
uvicorn>=0.40.0
websockets>=12.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# 数据库
sqlalchemy>=2.0.30
alembic>=1.13.0
aiomysql>=0.2.0
pymysql>=1.1.0
greenlet>=3.0.0

# Redis（redis 4+ 内置 asyncio 支持，无需 aioredis）
redis>=5.0.0

# 认证与安全
python-jose>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0,<4.1.0
cryptography>=42.0.0
python-multipart>=0.0.9
email-validator>=2.0.0

# HTTP 客户端
httpx>=0.27.0
aiohttp>=3.9.0

# Soul AI Lab (Gradio Space 调用)
gradio_client>=1.5.0

# 日志
loguru>=0.7.0

# 工具
python-dotenv>=1.0.0
duckduckgo-search>=8.0.0

# 测试（开发时按需安装：pip install pytest pytest-asyncio）
# pytest>=8.0.0
# pytest-asyncio>=0.23.0

# RAG/AI 扩展（可选）
# langchain>=0.2.0
# openai>=1.12.0
# pymilvus>=2.3.6

# Celery 任务队列（可选）
# celery>=5.3.6
